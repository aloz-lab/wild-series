<div class="bg-dark border-bottom border-secondary">
    <div class="container">
        <div class="row">
            <nav class="col my-2 navbar navbar-expand-lg bg-dark navbar-dark">
                <img src="/../assets/images/film-solid.png" width="40" height="30" alt="Site logo">
                <a class="navbar-brand text-danger" href="{{ path('app_index') }}">
                    Wild Series</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarContent">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div id="navbarContent" class="collapse navbar-collapse">
                    <ul class="navbar-nav">
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ path('app_index') }}">Home</a>
                        </li>
                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('wild_index') }}">Programs</a>
                            </li>
                        {% endif %}
                    </ul>
                    <form class="form-inline my-2 my-lg-0 ml-auto">
                        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                        <button class="btn btn-outline-danger my-2 my-sm-0" type="submit">Search</button>
                    </form>
                    <ul class="navbar-nav">
                        <li class="nav-item ml-5">
                            {% if app.user %}
                                <a class="btn btn-danger text-dark" href="{{ path('app_logout') }}" role="button">Se déconnecter</a>
                            {% else %}
                                <a class="btn btn-danger text-dark" href="{{ path('app_login') }}" role="button">Se connecter</a>
                            {% endif %}
                        </li>
                    </ul>
                </div>
            </nav>
        </div>
    </div>
</div>